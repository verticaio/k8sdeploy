pipeline {
    agent {
        label 'master'
    }

    stages {
        stage('Docker Login') {
            steps {
                echo 'Login to nexus repo...'
                sh 'ls -la'
            }} 

    }
}